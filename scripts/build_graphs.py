#!/usr/bin/env python3
"""CLI: preprocess raw data → feature graphs (Phase 2 stub)."""
import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


def main() -> None:
    logger.info("build_graphs: Phase 2 — not yet implemented")
    logger.info("Run after download_data.py completes")


if __name__ == "__main__":
    main()
